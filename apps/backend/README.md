# Backend

## Env
- Create `.env` from `.env.example`
- Required: `MONGODB_URI`, `TMDB_API_KEY`, `JWT_SECRET`, `JWT_REFRESH_SECRET`

## Commands
- `npm run dev`
- `npm start`

## Main routes
- `/api/auth/*`
- `/api/content/*`
- `/api/user/*`
